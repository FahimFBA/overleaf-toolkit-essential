# ==========================================================
# Overleaf Toolkit Essential - Environment Configuration
# ==========================================================

# Name of your running Overleaf Docker container
# (default from sharelatex/sharelatex image)
CONTAINER_NAME=sharelatex

# Absolute path to the toolkit project directory on your host machine
# Use an absolute path (avoid relative paths — cron will fail otherwise)
PROJECT_DIR=$HOME/tmp-overleaf/overleaf-toolkit-essential

# Log file path for cron-based updates and manual runs
# Logs are appended here each time the toolkit executes
LOG_FILE=$PROJECT_DIR/latex-update.log

# Path to the main LaTeX installer script
INSTALL_SCRIPT=$PROJECT_DIR/scripts/install-latex-packages.sh

# ----------------------------------------------------------
# Optional: Customize cron job timing (default: every Monday at 4 AM)
# Format: minute hour day month weekday
# Example: "0 2 * * *" = every day at 2 AM
# CRON_EXPR="0 4 * * 1"
# ----------------------------------------------------------
